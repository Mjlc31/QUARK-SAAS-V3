# Testing

- No formal testing framework (Jest, Cypress, Playwright) is currently configured or present in `package.json`.
- Testing is assumed to be manual.
- Components use `ErrorBoundary.tsx` for runtime error catching.
